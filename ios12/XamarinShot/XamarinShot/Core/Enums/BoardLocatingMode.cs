
namespace XamarinShot.Models.Enums {
	public enum BoardLocatingMode {
		WorldMap = 0, // default
					  // slot 1 previously used; leave empty so that on update,
					  // worldMap is used insead.
		Manual = 2,
	}
}
